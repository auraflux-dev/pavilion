/** Default newsletter CSS (mirrors newsletter-default.css). */
export const NEWSLETTER_DEFAULT_CSS = `/* SHMS PTO newsletter default */
.nl-wrap {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
  color: #1a1a1a;
}
.nl-header { background: #1b6b45; }
.nl-header-title {
  font-family: inherit;
  font-size: 16px;
  color: #ffffff;
}
.nl-section { padding-top: 28px; }
.nl-section-title {
  font-family: inherit;
  font-size: 18px;
  font-weight: 700;
  line-height: 1.3;
  color: #1a1a1a;
}
.nl-body {
  font-family: inherit;
  font-size: 15px;
  line-height: 1.55;
  color: #1a1a1a;
}
.nl-body a, .nl-footer a {
  color: #1b6b45;
  text-decoration: underline;
}
.nl-footer {
  font-size: 12px;
  line-height: 1.5;
  color: #5a6070;
  text-align: center;
}`
