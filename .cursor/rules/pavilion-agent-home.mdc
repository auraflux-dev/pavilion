---
description: Pavilion workspace — product author; SHMS is customer #1
alwaysApply: true
---

# Pavilion agent home (critical)

This workspace is **`pavilion`** (`auraflux-dev/pavilion`). **Pavilion is the product** (sold as Pavilion; legacy hosts may still say `commons-*`).

Ship the product app: **`commons-pto-demo`**. Brand packs skin prospects. See `commons-one-surface.mdc`.

## Source of truth

| Work | Where |
|------|--------|
| Shared product (site, portal, staff) | **`~/pavilion`** — author here |
| Demo / brand packs / trial / `commons-site` | **`~/pavilion`** |
| SHMS production customer | **`~/shmspto`** — promote when intentional; hotfix-only edits |

## Vercel wall (never mix)

| Account | Use for |
|---------|---------|
| **robert-4220s-projects** | Pavilion: `commons-pto-demo`, `commons-pto`, `commons-site` |
| **treasurer-4353s-projects** | SHMS www only via **`~/shmspto`** git push. **Never** from this monorepo |

## Promote / sync

**Expected:** pavilion → SHMS (customer promote):

```bash
node scripts/promote-to-shms.mjs
```

**Emergency only:** SHMS hotfix → pavilion (`--from-shms`) same day.

Do **not** promote to live SHMS unless Rob asks or it is an agreed customer ship. **School-hours sensitivity:** www is production.

## Anti-patterns (forbidden)

- Authoring shared product only in `~/shmspto`
- Auto-promote after every Pavilion ship
- Pushing pavilion and calling www “live”
- Treasurer `vercel` from this repo
